import {
  wrapPageElement as _wrapPageElement,
} from './src/utils/providers';

/** Wrap each page in the shared provider tree during static rendering. */
export const wrapPageElement = _wrapPageElement;
